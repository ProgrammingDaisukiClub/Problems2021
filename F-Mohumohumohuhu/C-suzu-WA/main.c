#include <stdio.h>

int main() {
    int T;
    scanf("%d", &T);
    char S[1001];
    while (T--) {
        int N;
        scanf("%d", &N);
        scanf("%s", S);
        printf("No\n");
    }
    return 0;
}